define([
    'text!app/components/contact/contact.html'
], function (template) {

    // Create component class
    var Contact = {
        name: 'Contact',
        template: template
    };
    return Contact;
});

