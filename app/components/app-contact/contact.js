define([
    'text!app/components/app-contact/contact.html'
], function (template) {

    // Create component class
    var Contact = {
        name: 'app-contact',
        template: template
    };
    return Contact;
});

